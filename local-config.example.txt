# 复制本文件为 local-config.js（与 OAO.html 同目录）



// 工作区名称（与 AnythingLLM 一致，默认 OAOeth）

window.OAO_ANYTHINGLLM_WORKSPACE = 'oaoeth';

// 本地 AI 统一网关（AnythingLLM 3002 / Ollama / SearXNG 都经网关 3001）
window.OAO_AI_BASE_URL = 'http://127.0.0.1:3001';



// AnythingLLM → Settings → API Keys → 生成后粘贴：

window.OAO_ANYTHINGLLM_API_KEY = '你的-AnythingLLM-API-Key';



// Cloudflare Worker（外网 Tunnel）

// window.OAO_WORKER_URL = 'https://oao-ai.wh529007.workers.dev';



// ========== AI 模式（本地覆盖配置，不提交 GitHub）==========
// 本地 AI 统一走 OAO AI 网关（默认 http://127.0.0.1:3001）。
// 外网站点统一走 Cloudflare Worker，无需在本文件写 Worker URL。
// 如果想让外网站点不依赖本机，改为使用 Cloudflare Worker + 智谱 GLM：
// window.OAO_FORCE_GLM = true;

// Ollama 也走网关，不要直接写 127.0.0.1:11434。
window.OAO_OLLAMA_BASE_URL = 'http://127.0.0.1:3001/ollama';


// ========== AI联网 增强（推荐，SearXNG/Docker 不可用时仍可用）==========

// Serper — Google 搜索 API，免费约 2500 次/月

// 注册: https://serper.dev  →  Dashboard 复制 API Key

// window.OAO_SERPER_API_KEY = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx';



// 也可在 Worker 配置（外网 GitHub Pages 用户推荐）：

//   cd cloudflare

//   npx wrangler secret put SERPER_API_KEY



// Tavily（可选）https://tavily.com

// window.OAO_TAVILY_API_KEY = 'tvly-xxxxxxxx';
